// features/workspace/hooks/useSocket.js
// Stage 5 — placeholder only. Full implementation in Stage 7.
// DO NOT connect socket here yet.

export function useSocket() {
  return {
    isConnected: false,
    // Stage 7: connectSocket(repoId), disconnectSocket(), emit(), on()
  }
}